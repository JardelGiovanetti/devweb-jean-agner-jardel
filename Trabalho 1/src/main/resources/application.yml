spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: devweb
    url: jdbc:mysql://localhost:3306/devweb
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
